# libasm

Réimplémentation en assembleur x86-64 (syntaxe Intel, NASM) de quelques
fonctions de la libc, regroupées dans une bibliothèque statique `libasm.a`.

## Compilation

```bash
make
```

## Tests

```bash
make test
```